# dependencies
node_modules/

# Next.js build output
.next/
out/

# env files — never commit secrets
.env
.env.local
.env.*.local

# TypeScript build info
tsconfig.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# debug logs
npm-debug.log*

.vercel
.env*

# Higgsfield skills (installées via `npx skills add higgsfield-ai/skills`,
# régénérables depuis skills-lock.json — .claude/skills/ contient des
# symlinks à chemins absolus, non portables d'une machine à l'autre)
.agents/
.claude/skills/
